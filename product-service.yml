spring:
  application:
    name: product-service

server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/pos_product
    username: root
    password: Root@0710
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"