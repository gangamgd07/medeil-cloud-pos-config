server:
  servlet:
    encoding:
      charset: UTF-8

springdoc:
  api-docs:
    path: /api-docs

  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: "*"

jwt:
  secret: pharmacyCloudSecretKeyForJwtAuthentication2026VeryStrongKey123
